# Ques of	OPS

1. 服务中心是租户行为,其中包括：

   * 实时sql

     ```
     http://192.168.181.215:61505/ops_server/api/v1/statements?orderby=querystart
     ```

   * 业务自检

     ```
     http://192.168.181.215:61505/ops_server/api/v1/haveRunningDiagnoses (第一次点击业务自检的时候)		
     ```

   * 冒烟测试

2. 告警中心也是租户行为，包括：

   * 告警信息
   * 告警设置

3. 日志中心也是租户行为，包括：

   * 服务日志

4. 用户中心也是租户行为，包括：

   * 用户管理
   * 资源队列

5. 还有就是租户的运维大盘，也是租户行为，包括：

   * xxx





租户管理员：

运维大盘：

* 上面显示的数据都有哪些？
* *集群节点结构图底层依赖的ambari？*
* 告警如何做？


集群管理员：

1. 运维大盘：
   1. 获取控制节点，计算节点，租户数量，剩余资源等；
   2. 获取集群中所有的节点结构图；
   3. 操作日志；
2. 集群管理：
   1. ​
3. 告警中心的设置：
4. ​


---

在通过rm获取租户的拓扑结构的时候没有primary 和mirror的信息；


